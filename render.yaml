services:
  - type: web
    name: my-line-bot
    runtime: bun
    buildCommand: bun install
    startCommand: bun run index.ts
    envVars:
      - key: LINE_CHANNEL_ACCESS_TOKEN
        sync: false
